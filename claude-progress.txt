# Claude Progress Log
#
# Append one dated entry per Claude Code session summarizing what was done,
# decisions made, and any unresolved issues. Newest entries at the bottom.

## 2026-08-29 - GitHub Issue #6: 棒グラフの作成

- `bar-chart`(表示専用)と `bar-chart-driver`(ダミーデータ生成)コンポーネントを新規作成。
  - `bar-chart` は複数パラメータ(`BarChartParam[]` + `ChartPointData[]`)を1つの棒グラフとして描画。
    Y軸は1本のみ(左側固定)で、`yAxisRange` を指定すると固定レンジ、未指定ならECharts標準の
    自動レンジになる。モードの動的切り替えは非サポート(要件通り)。
  - グラフの表示サイズは呼び出し側がホスト要素に CSS の width/height(および
    max-width/min-width)を指定する方式(`:host { display: block }` + 内部 div を 100%/100%)。
    既存の single-bar-chart / cylinder-chart-box が採用しているピクセル数値の `width`/`height`
    input とは異なる方式を意図的に採用(Issue #6 の要求に合わせた)。ngx-echarts の
    `autoResize`(デフォルト true, ResizeObserver ベース)でサイズ追従することを確認済み。
  - `bar-chart-driver` は「静的データ + 固定レンジ」と「setInterval で動的に変化する値 + 自動レンジ」
    の2チャートを並べて表示し、Issue の受け入れ条件(静的描画・動的描画・アニメーション・
    レスポンシブサイズ・Y軸位置・レンジ2モード)をすべて1画面でデモできるようにした。
  - `model/chart-data.interface.ts` に `BarChartParam`(name + color のみ、yAxisIndex なし)を追加。
    既存の `ChartSeriesData` は系列ごとに Y 軸を持つ前提のため、単一共有 Y 軸の bar-chart には
    転用せず新規インターフェースにした。
  - ルーティング(`app.routes.ts`)とナビゲーション(`app.ts`)に `bar-chart` パスを追加。
- 動作確認: `npm run build` / `npm test`(18 tests green)に加え、Playwright で dev server 上の
  `/bar-chart` をスクリーンショット・aria-label・レスポンシブ幅(ビューポート 500px で
  min-width 280px が効くこと)を目視確認済み。コンソールエラーなし。
- 未解決事項なし。ブランチ `feature/6-bar-chart` で PR 作成予定。

## 2026-08-29 - GitHub Issue対応時のテストエビデンス添付フロー整備

- GitHub Issueに基づく修正作業を完了した際、テストケースごとのエビデンス(スクリーンショット/
  入出力パターン)をzip化してGoogle Drive経由でIssueに紐づける運用を追加。GitHub Issuesの公式
  API/`gh` CLIには任意ファイルをIssueへ直接添付する公開エンドポイントが存在しない(Web UIの
  ドラッグ&ドロップは非公開の内部API)ため、本人のGoogle Driveにアップロードし共有リンクを
  Issueコメントに記載する方式を採用(ユーザー確認済み)。
- `scripts/init-firewall.sh`: allowlistに `oauth2.googleapis.com` / `www.googleapis.com` を追加。
- `scripts/init-verify-gdrive.sh`(新規): `GDRIVE_CLIENT_ID` / `GDRIVE_CLIENT_SECRET` /
  `GDRIVE_REFRESH_TOKEN` / `GDRIVE_FOLDER_ID` の疎通確認。未設定時は警告してexit 0でスキップし、
  他の検証ステップをブロックしない。`init.sh` のstep配列に追加。
- `scripts/attach-issue-evidence.sh`(新規): `tc1`, `tc2`... という名前のフォルダ(各々
  `title.txt` + エビデンス本体)を受け取り、zip化 → Google Driveへアップロード → 各ファイルを
  リンク共有可能に設定 → 全テストケースのタイトル+リンクをまとめた1コメントを
  `gh issue comment` で投稿、までを行う。依存は curl/jq/zip/gh のみ。
- `CLAUDE.md`: Session End ProcedureにGitHub Issue起点の作業のみ適用される条件付きステップを
  追加(step 2)。Issueを参照しない作業では発動しない。Session Start Procedureの説明文も
  init.shの新ステップに合わせて更新。
- 動作確認: 新規/変更した4スクリプトを `bash -n` で構文チェック。`init-verify-gdrive.sh` は
  GDRIVE_*未設定の状態で実行し、警告メッセージとexit 0を確認。`attach-issue-evidence.sh` は
  引数なし/`title.txt`欠如/存在しないディレクトリの各異常系がエラー終了することを確認。
  `npm test`(18 tests green)。
- 未解決事項: Google Drive側の実際のOAuthクレデンシャル(GDRIVE_CLIENT_ID等)はユーザーが
  一度だけ手動でセットアップし `.devcontainer/secrets.env` に追加する必要があり、今回のセッション
  では未設定・未実施(手順は `scripts/attach-issue-evidence.sh` のヘッダーコメントに記載)。
  実際のGoogle Driveアップロード〜Issueコメント投稿のエンドツーエンド確認は、その設定完了後に
  別途実施が必要。

## 2026-08-30 - Google Drive OAuth設定 & attach-issue-evidence.sh のエンドツーエンド確認

- ユーザーがGoogle Cloud ConsoleでOAuthクライアント(ウェブアプリケーション種別。デスクトップ
  アプリ種別だとOAuth Playgroundの `redirect_uri_mismatch` エラーになるため変更)を作成し、
  OAuth Playground経由で `drive.file` スコープのrefresh tokenを取得、`.devcontainer/secrets.env`
  に `GDRIVE_CLIENT_ID` / `GDRIVE_CLIENT_SECRET` / `GDRIVE_REFRESH_TOKEN` / `GDRIVE_FOLDER_ID`
  を設定してコンテナを再起動。
- `bash scripts/init-verify-gdrive.sh` で疎通確認 (`mitsuru.takahashi.biz@gmail.com` として認可済み)。
- テスト用Issue(#9、確認後クローズ済み)を作成し、ダミーの `tc1`(PNGスクリーンショット想定)/
  `tc2`(入出力パターンのテキスト)フォルダで `scripts/attach-issue-evidence.sh` をエンドツー
  エンドで実行。zip化 → Google Driveアップロード → リンク共有設定(認証なしでHTTP 200到達を確認)
  → Issueへの1コメント投稿、まで正常動作を確認。
- 実行時に `zip` コマンドがコンテナイメージに含まれていないことが判明したため、`sudo apt-get
  install zip` でその場対応しつつ、`Dockerfile` にも `zip` を恒久的に追加(次回イメージ再ビルド時
  も確実に使えるように)。
- テスト後の後片付けとして、Google Drive上のテスト用zip 2件をAPI経由で削除。
- `npm test`(18 tests green)で回帰なしを確認。
- 未解決事項なし。GitHub Issue起点の修正作業の際は、今回整備したフローで実運用可能な状態。

## 2026-08-30 - GitHub Issue #8: Playwrightのテストレポートにアクセスできない

- 原因調査: `npm run e2e`(`ng e2e` → playwright-ng-schematics のbuilder → 内部で
  `npx playwright test` を子プロセスとして起動)完了時、`html` reporter がデフォルトの
  `host: "localhost"` でHTMLレポートサーバー(ポート9323)を起動していた。この
  バインド先はループバック(`::1`)のみで、コンテナ外(ホストPCのブラウザ/curl)から
  到達不可。`angular.json` の dev-server が同じ理由で `host: "0.0.0.0"` を明示指定して
  いるのと全く同じ構図(このコンテナ環境ではポート公開がループバック非対応)。
  実機確認: `/proc/net/tcp6` で `00000000000000000000000001000000:246B`(`::1:9323`)の
  リッスンを確認し、原因を特定。
- 修正: `playwright.config.ts` の `reporter: 'html'` を
  `reporter: [['html', { host: '0.0.0.0' }]]` に変更。これにより `npm run e2e` 完了後に
  自動オープンされるレポートサーバー、および `npx playwright show-report --host 0.0.0.0`
  で手動再表示する場合の双方が全インターフェース(`0.0.0.0`)でリッスンするようになる。
- 動作確認: `npx playwright show-report --host 0.0.0.0` 実行中に `/proc/net/tcp` で
  `00000000:246B`(`0.0.0.0:9323`)のリッスンおよび `curl http://127.0.0.1:9323/` が
  200を返すことを確認(修正前は `0100007F`=ループバックのみでリッスンしていたことも
  併せて確認・記録)。`npm test`(18 tests green)、`npm run build` も回帰なし。
- Issue本文に明記の通り、既存e2eテスト自体の失敗(dev server未起動によるもの)は
  本作業の対象外(修正していない)。
- テストエビデンス(バインドアドレスのbefore/after比較とcurl結果)をGoogle Drive経由で
  Issue #8にコメント添付済み。
- 未解決事項: `npx playwright show-report` をユーザーが手動実行する場合、CLIの
  `--host` デフォルトは`localhost`のまま(playwright.config.tsのreporter設定を
  読まない、Playwright側の仕様)なので、その場合は `--host 0.0.0.0` を明示的に
  付与する必要がある。今回のIssueの再現手順・受け入れ条件(`npm run e2e` 実行後の
  自動表示レポートへのアクセス)には影響しないため対応は見送ったが、必要であれば
  npm scriptを別途追加する等のフォローアップが考えられる。

## 2026-08-30 - 作業ブランチ/PRワークフローの導入 と reporter サブエージェント新設

- `CLAUDE.md` に新セクション「Working Branch & Pull Requests」を追加。以後、コード変更前に
  必ず `scripts/start-work-branch.sh <issue-number|none> <feat|fix> [<base-branch>]` で
  作業ブランチを作成/再開してからコミットするルールとした(`main` への直接コミットを禁止)。
  ブランチ名は `${type}/${issue-number}/claude-code` 形式で機械的に決定
  (例: `feat/issue-10/claude-code`, `fix/issue-322/claude-code`, `feat/2026-08-30/claude-code`)。
  - `${type}` はIssueのラベルから優先順位 `enhancement`→`feat`, `bug`→`fix` で自動判定し、
    ラベルなし/Issueなしの場合のみ呼び出し側(メインエージェント)が指定した `<feat|fix>` に
    フォールバックする。
  - `${issue-number}` はIssueがあれば `issue-<n>`、なければ現在日時ベースの `yyyy-MM-dd`。
  - ベースブランチ未指定時は常に最新の `origin/main` から分岐。
- `scripts/start-work-branch.sh` を新規作成(ブランチ名の機械的決定・作成・再開ロジック)。
- `scripts/commit-push-pr.sh` を新規作成。指定ファイルのコミット・push・
  オープン中PRの有無チェック(`gh pr list --head <branch> --state open`)・
  未オープンならPR新規作成、を一括で行う。既にオープンPRがあれば重複作成しない。
- 新規サブエージェント `.claude/agents/reporter.md` を追加(`model: haiku`、低コストモデル)。
  Session End Procedureの機械的な後処理((1)Issueエビデンスのzip化/アップロード/コメント、
  (2) claude-progress.txt / feature-list.json の更新、(3) コミット・push・PR作成)を担当。
  コミットメッセージ・進捗ログ本文・PRタイトル/本文などの「何を書くか」の判断は一切行わず、
  メインエージェントから渡された内容をそのまま実行するだけの設計(判断はメイン側に集約)。
- `CLAUDE.md` の Session End Procedure を、上記に合わせて再構成
  (step1: npm test は従来通りメインが実施 / step2: 進捗ログ・feature-list更新内容・
  コミットメッセージ・PRタイトル/本文などをメインが起草 / step3: reporter サブエージェントに
  実行を委譲)。
- 動作確認: 本セッションの変更自体をこの新フローで運用し、`scripts/start-work-branch.sh none feat`
  で `feat/2026-08-30/claude-code` ブランチを作成(このIssueなし・新機能追加のケース)。
  `npm test`(18 tests green)を確認。
- 未解決事項: `reporter` サブエージェントは本セッション中に新規作成したため、このセッション
  自体ではエージェント定義がまだロードされておらず(セッション起動時に読み込まれる設計)、
  今回のコミット・push・PR作成はメインエージェントが直接 `scripts/commit-push-pr.sh` を
  実行して代行した。`reporter` サブエージェント自体の動作確認は次回セッション以降に持ち越し。
  本セッションの変更はアプリの `feature-list.json` 記載の機能(bar-chart等)には該当しない
  ワークフロー/ツール整備のため、`feature-list.json` の更新は無し。

## 2026-08-30 - GitHub Issue #10 動作確認: 作業ブランチ/PRワークフローの検証

- Issue #10は、前回セッション(コミット `ba90b05`, PR #11)で既に実装済みだった「作業ブランチ作成
  → PR作成」ワークフロー(`scripts/start-work-branch.sh` / `scripts/commit-push-pr.sh` /
  `reporter`サブエージェント)を追跡するチケットだったため、本セッションでは新規コード変更ではなく
  動作確認とレポーティングのみを実施した。
- 動作確認内容:
  - `bash -n` で `scripts/start-work-branch.sh` / `scripts/commit-push-pr.sh` の構文を再確認。
  - ラベル→type判定を実Issueに対して確認: #10(`enhancement`)→`feat`、#8(`bug`)→`fix`、
    #9(ラベルなし)→呼び出し側指定のフォールバック。いずれも設計通り。
  - `scripts/start-work-branch.sh none feat` を実行し、既に対象ブランチ
    (`feat/2026-08-30/claude-code`、今日の日付と一致)上にいる場合は再作成・再フェッチをせず
    即座に成功終了することを確認(`git status --short` に差分なし、副作用なし)。
  - `gh pr list --head feat/2026-08-30/claude-code --state open` で、`commit-push-pr.sh` が
    参照する「既存オープンPR」検知ロジックがPR #11を正しく検知することを確認し、重複PR作成が
    起きない実装であることをスクリプト該当箇所と合わせて確認した。
  - `npm test`(18 tests green)を`init.sh`実行時に確認。
- Issue #10 の受け入れ条件記載のブランチ名 `feat/issue-10/branch-control-by-claude` と、実際に
  CLAUDE.md/スクリプトで確定した命名規則(`${type}/issue-<n>/claude-code`)との間に差異がある
  ことをユーザーに確認し、「既存のPR #11をそのまま採用し、ブランチの作り直しは行わない」方針で
  合意した。
- PR #11 の本文に `Closes #10` を追記してIssue #10と正式に紐付け、検証結果のエビデンス
  (ラベル→type判定、ブランチ再開パス、PR再利用判定の入出力ログ)をIssue #10にコメント添付した。
- 未解決事項なし。
